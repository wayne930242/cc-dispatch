# Workflow Summary

**Date:** 2026-04-21 16:29

## Analysis Report Reference

- Report path: none (maturity=None, no existing agent system)
- Key findings addressed: n/a

## Pipeline Mode Mapping

| Workflow | Mode | Steps | Entry Point | State Management | Script Needed | Rationale |
|----------|------|-------|-------------|------------------|---------------|-----------|
| Implement feature / refactor | owner-pipe | situational (2-4) | situational | stateless | no | Solo dev, small scope, single session completion |
| Fix bug / failing test | owner-pipe | situational (2-4) | situational | stateless | no | Same as above — human decides next step per task |
| Release preparation | owner-pipe | fixed (4) | `releasing` skill invocation | stateless | no | CHANGELOG → README → secret scan → tag |

## Pain Points

| Pain Point | Root Cause | Suggested Fix | Component Type | Priority |
|------------|------------|---------------|----------------|----------|
| Platform-specific code breaks cross-platform builds | OS-specific behavior without build tag isolation (prior incident: commit 6c6a80d skipped Unix-specific tests on Windows) | Enforce build tags for any platform-branching code | Rule (path-scoped) | HIGH |
| Breaking changes to daemon HTTP protocol or sqlite schema | Protocol/schema edits without migration or version gate; would break MCP clients and existing user state in `~/.cc-dispatch/` | Require explicit migration for schema; freeze protocol shape unless intentional versioned change | Rule (path-scoped) | HIGH |

## Routine Tasks

| Task | Frequency | Current Process | Automation Approach | Component Type |
|------|-----------|-----------------|---------------------|----------------|
| Format Go files | Every edit | Manual `gofmt` / `goimports` | Auto-run on `*.go` edit via PostToolUse hook | Hook |
| Static check Go files | Every edit | Manual `go vet` | Auto-run on `*.go` edit via PostToolUse hook | Hook |
| Full lint | Pre-PR | `golangci-lint run` | CI only (slow for per-edit hook) | CI (out of scope) |
| Run tests | Before commit | `go test ./...` | Direct command; documented in CLAUDE.md verification section | CLAUDE.md instruction |
| Release prep | Per tag | Manual CHANGELOG + README + secret check | `releasing` skill runs the 4-step ritual | Skill |

## Component Recommendations

| Component | Type | Action | Traced To | Priority |
|-----------|------|--------|-----------|----------|
| `CLAUDE.md` | CLAUDE.md | create | Project entry; communication (reply in Traditional Chinese); verification commands | HIGH |
| `.claude/rules/cross-platform.md` | rule | create | Pain point: platform-specific code without build tags | HIGH |
| `.claude/rules/daemon-stability.md` | rule | create | Pain point: daemon HTTP protocol + sqlite schema stability | HIGH |
| `.claude/hooks/go-format-vet.sh` (or similar) + settings registration | hook | create | Routine: gofmt + goimports + go vet on `*.go` edits | MEDIUM |
| `.claude/skills/releasing/SKILL.md` | skill | create | Routine: release prep (CHANGELOG → README → secret scan → tag) | MEDIUM |

## Workflow Pattern Mapping

| Workflow | Anthropic Pattern | Skill Routing | Complexity Level | Rationale |
|----------|------------------|---------------|-----------------|-----------|
| Implement / fix | Autonomous Agent | no skill (direct work + rules + hook) | L2-L3 | Situational owner-pipe, small scope — doesn't need a workflow skill |
| Release prep | Prompt Chaining | `releasing` skill, auto-invoke internal steps | L4 | Fixed 4-step ritual fits prompt chaining |
| Review / verify | Evaluator-Optimizer (auto loop) | no standalone skill; ad-hoc | L1 | User confirmed: auto-loop preference, but not yet formalized as a skill |

## Human Intervention Points

| Workflow Step | Intervention Type | Trigger Condition | Component Impact |
|---------------|-------------------|-------------------|------------------|
| Quality gate (review → fix) | auto loop | Any issue found by verifier | Skill chain uses `auto-invoke` handoff (no user-confirmation between review and fix) |
| Release tag push | n/a (no gate) | User chose to trust agent for local ops | `releasing` skill proceeds without confirmation gate; global `git-safety` rule still applies for force push |

## Past Failures & Constraints

- **Prior cross-platform incident**: commit `6c6a80d test: skip Unix-specific tests on Windows` — motivates cross-platform rule.
- **Zero-runtime-dependency promise**: single static binary, no Node/Python. Constrains future tooling choices.
- **Existing release pipeline**: GoReleaser (`.goreleaser.yaml` + `.github/workflows/release.yml`); release-please to be layered on top (per `.rcc/config.yml`).
- **Solo project, owner-pipe preference**: user reviews own diffs, no formal review skill needed.
- **Language convention**: skill/rule/CLAUDE.md files in English; CLAUDE.md communication rule mandates Traditional Chinese replies.
